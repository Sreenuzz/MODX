import 'package:flutter/material.dart';

class AttendanceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Verify Attendance'),
      ),
      body: Center(
        child: Text('Attendance Verification Page'),
      ),
    );
  }
}
